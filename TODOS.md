# TODOS

- [ ] **P4 — Real Run Analysis trigger in the Design Lab (deferred from v5 scope ceremony):** In report-first and stream worlds, Run Analysis should POST to the studio's real /analyze endpoint and stream the 12 pipeline stages with live progress (the app already has /analyze + /progress + stall-heal). Deferred because minutes-long runs make design review tedious; the pages currently show a recorded stage sequence in their lifecycle states.
  - Depends on: nothing blocked; wire after the winning IA is ported, or on request.
