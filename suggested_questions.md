## Suggested Questions
_Questions this graph is uniquely positioned to answer:_

- **Why does `ScriptDocument` connect `Parser & Knowledge Graph` to `CLI & Formatting`, `Docstrings & Comments`, `Evidence Verifier`?**
  _High betweenness centrality (0.244) - this node is a cross-community bridge._
- **Why does `ProjectManifest` connect `Studio Manifest & Tests` to `Docstrings & Comments`, `E2E Tests`, `Web Tests`?**
  _High betweenness centrality (0.180) - this node is a cross-community bridge._
- **Why does `Orchestrator` connect `Docstrings & Comments` to `Parser & Knowledge Graph`, `E2E Tests`, `Studio Manifest & Tests`?**
  _High betweenness centrality (0.136) - this node is a cross-community bridge._
- **Are the 41 inferred relationships involving `ProjectManifest` (e.g. with `CLI for screenplay_studio.  Usage:     # full pipeline: parse -> analyze -> drop` and `Hands off to the same interactive loop Piece 3's own CLI uses.`) actually correct?**
  _`ProjectManifest` has 41 INFERRED edges - model-reasoned connections that need verification._
- **Are the 33 inferred relationships involving `Orchestrator` (e.g. with `CLI for screenplay_studio.  Usage:     # full pipeline: parse -> analyze -> drop` and `Hands off to the same interactive loop Piece 3's own CLI uses.`) actually correct?**
  _`Orchestrator` has 33 INFERRED edges - model-reasoned connections that need verification._
- **Are the 34 inferred relationships involving `ScriptDocument` (e.g. with `CLI for screenplay_studio.  Usage:     # full pipeline: parse -> analyze -> drop` and `Formatting & industry-standard compliance checks — deterministic, no model call.`) actually correct?**
  _`ScriptDocument` has 34 INFERRED edges - model-reasoned connections that need verification._
- **Are the 20 inferred relationships involving `LlamaServerClient` (e.g. with `CLI for screenplay_studio.  Usage:     # full pipeline: parse -> analyze -> drop` and `_NullRulesContext`) actually correct?**
  _`LlamaServerClient` has 2